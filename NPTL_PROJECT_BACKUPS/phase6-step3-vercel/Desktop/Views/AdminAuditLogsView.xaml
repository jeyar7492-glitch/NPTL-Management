<UserControl x:Class="NPTELManagement.Desktop.Views.AdminAuditLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:common="clr-namespace:NPTELManagement.Desktop.Common"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100"
             Background="{StaticResource BackgroundBrush}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <common:InverseBooleanToVisibilityConverter x:Key="InvBoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 1. Header & Actions -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="SECURITY &amp; COMPLIANCE" FontSize="11" FontWeight="Bold" Foreground="{StaticResource SecondaryBrush}" Margin="0,0,0,4"/>
                    <TextBlock Text="System Audit Logs &amp; Operational Ledger" FontSize="22" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                    <TextBlock Text="{Binding TotalCount, StringFormat='Total Logged Events: {0} (Read-Only Immutable Audit Trail)'}" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="↻ Refresh Logs" Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}" Padding="14,8"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 2. Filters & Search Bar -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16" Padding="16,12">
            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                <StackPanel Margin="0,0,16,8">
                    <TextBlock Text="Search (User / Action / Details)" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="240" Style="{StaticResource FormTextBox}"/>
                </StackPanel>

                <StackPanel Margin="0,0,16,8">
                    <TextBlock Text="Action Type" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                    <ComboBox SelectedValue="{Binding SelectedAction, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content" Width="180" Style="{StaticResource FormComboBox}">
                        <ComboBoxItem Content="All"/>
                        <ComboBoxItem Content="STUDENT_CREATE"/>
                        <ComboBoxItem Content="STUDENT_UPDATE"/>
                        <ComboBoxItem Content="STUDENT_STATUS_UPDATE"/>
                        <ComboBoxItem Content="STAFF_CREATE"/>
                        <ComboBoxItem Content="STAFF_UPDATE"/>
                        <ComboBoxItem Content="STAFF_PASSWORD_RESET"/>
                        <ComboBoxItem Content="STAFF_STATUS_UPDATE"/>
                        <ComboBoxItem Content="COURSE_CREATE"/>
                        <ComboBoxItem Content="COURSE_UPDATE"/>
                        <ComboBoxItem Content="COURSE_STATUS_UPDATE"/>
                        <ComboBoxItem Content="REGISTRATION_CREATE"/>
                        <ComboBoxItem Content="REGISTRATION_STATUS_UPDATE"/>
                        <ComboBoxItem Content="EXAM_UPDATE"/>
                        <ComboBoxItem Content="CERTIFICATE_UPLOAD"/>
                        <ComboBoxItem Content="CERTIFICATE_VERIFY"/>
                        <ComboBoxItem Content="NOTIFICATION_DISPATCH"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Margin="0,0,16,8">
                    <TextBlock Text="Role" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                    <ComboBox SelectedValue="{Binding SelectedRole, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content" Width="100" Style="{StaticResource FormComboBox}">
                        <ComboBoxItem Content="All"/>
                        <ComboBoxItem Content="Admin"/>
                        <ComboBoxItem Content="Staff"/>
                        <ComboBoxItem Content="Student"/>
                        <ComboBoxItem Content="System"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Margin="0,0,16,8">
                    <TextBlock Text="From Date" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding DateFrom}" Width="120" Style="{StaticResource FormDatePicker}"/>
                </StackPanel>

                <StackPanel Margin="0,0,16,8">
                    <TextBlock Text="To Date" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding DateTo}" Width="120" Style="{StaticResource FormDatePicker}"/>
                </StackPanel>
            </WrapPanel>
        </Border>

        <!-- 3. Audit Logs DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
            <Grid>
                <DataGrid ItemsSource="{Binding AuditLogs}"
                          SelectedItem="{Binding SelectedLog}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          Background="White"
                          RowHeight="40"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat='dd MMM yyyy HH:mm:ss'}" Width="150"/>
                        <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="180" FontWeight="Bold"/>
                        <DataGridTextColumn Header="User" Binding="{Binding Username}" Width="120"/>
                        <DataGridTemplateColumn Header="Role" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" Background="#EEF2F6">
                                        <TextBlock Text="{Binding Role}" FontSize="11" FontWeight="SemiBold" Foreground="#334155"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}" Width="110"/>
                        <DataGridTextColumn Header="Audit Event Details" Binding="{Binding Details}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Grid Background="#66FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="Loading audit trail..." HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}"/>
                </Grid>
            </Grid>
        </Border>

        <!-- 4. Status Bar -->
        <StackPanel Grid.Row="3" Margin="4,12,4,0" Orientation="Horizontal">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold"/>
        </StackPanel>

    </Grid>
</UserControl>
